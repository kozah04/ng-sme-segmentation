
# Nigerian SME Segmentation

Unsupervised segmentation of Nigerian SMEs using K-Means,

hierarchical clustering, and PCA. No target variable, this is a discovery task.

## Setup

conda env create -f environment.yml

conda activate ng-sme-segmentation

## Data

Place raw data files in data/raw/ (gitignored).

